apiVersion: v1
kind: PersistentVolume
metadata:
  name: liferay-pv
spec:
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: manual  # ✅ Added this
  hostPath:
    path: "/mnt/data/liferay-db"
