apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: liferay-pvc
  namespace: default
  labels:
    app: liferay
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: manual
